/*
var bindModel = function(data)
{

    var bots = data.bots,
	    token = data.token,
		session = data.session; 
	//model = $.parseJSON(data);
	//$(".banner").html(buildBannerView(true));
	//addBotViews();
}
*/

//var Model = ko.observableArray(ko.fromJSON());